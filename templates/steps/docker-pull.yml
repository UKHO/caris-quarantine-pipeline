parameters:
  - name: displayName
    type: string
    default: 'Pull Image'
  - name: imageName
    type: string

steps:
- task: PowerShell@2
  displayName: ${{ parameters.displayName }}
  inputs:
    targetType: 'inline'
    script: |
      docker pull ${{ parameters.imageName }}
