parameters:
  - name: displayName
    type: string
    default: 'Login to Registry'
  - name: containerRegistry
    type: string

steps:
- task: Docker@2
  displayName: ${{ parameters.displayName }}
  inputs:
    containerRegistry: ${{ parameters.containerRegistry }}
    command: 'login'
